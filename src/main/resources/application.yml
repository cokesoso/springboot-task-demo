spring:
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://localhost:27017/demo}

server:
  port: ${SERVER_PORT:8080}

app:
  jwt:
    secret: ${JWT_SECRET:demo-jwt-secret-key-change-in-production-must-be-at-least-256-bits-long!!}
    expiration-ms: ${JWT_EXPIRATION_MS:86400000}

logging:
  level:
    root: INFO
